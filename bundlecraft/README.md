

3. **Set Environment Variables** in the DO dashboard:
   ```
   NODE_ENV=production
   DATABASE_URL=(auto-generated by DO)
   SESSION_SECRET=your-secure-random-string
   STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key
   STRIPE_PUBLISHABLE_KEY=pk_live_your_stripe_publishable_key
   PRINTIFY_API_TOKEN=your_printify_token
   OPENAI_API_KEY=your_openai_key
   ```

### Option 2: Docker Deployment

```bash
# Clone the repository
git clone your-repo-url
cd bundlecraft

# Copy environment file and configure
cp .env.example .env
# Edit .env with your actual values

# Build and run with Docker Compose
docker-compose up -d
```

## 🔧 Local Development

```bash
# Install dependencies
npm install

# Run in development mode
npm run dev

# Build for production
npm run build

# Start production server
npm run start
```

## 🔐 Admin Access

Login with your admin credentials:
- **Email**: joan@example.com
- **Password**: your-password

## 📋 Features

- ✅ **Printify Integration**: Import products directly from Printify
- ✅ **Bundle Creation**: Create profitable product bundles
- ✅ **Etsy Optimization**: Generate SEO-optimized listings
- ✅ **Stripe Payments**: Process payments with live Stripe keys
- ✅ **Admin Dashboard**: Manage products and bundles
- ✅ **Export Tools**: Export bundle data for Etsy listings

## 🛠 Tech Stack

- **Frontend**: React + TypeScript + Vite
- **Backend**: Node.js + Express + TypeScript
- **Database**: PostgreSQL
- **Authentication**: Session-based admin auth
- **Payments**: Stripe (live keys supported)
- **Deployment**: Digital Ocean App Platform

## 📦 Environment Variables

| Variable | Description | Required |
|----------|-------------|----------|
| `NODE_ENV` | Application environment | Yes |
| `DATABASE_URL` | PostgreSQL connection string | Yes |
| `SESSION_SECRET` | Session encryption key | Yes |
| `STRIPE_SECRET_KEY` | Stripe secret key (live) | Yes |
| `STRIPE_PUBLISHABLE_KEY` | Stripe publishable key (live) | Yes |
| `PRINTIFY_API_TOKEN` | Printify API token | Yes |
| `OPENAI_API_KEY` | OpenAI API key | Yes |


## 📞 Support

For deployment issues:
1. Check the Digital Ocean build logs
2. Verify all environment variables are set
3. Ensure your Stripe keys are live keys
4. Check database connectivity

## 🚀 Ready for Production

This application is production-ready with:
- Optimized build process
- Secure session management
- Live Stripe integration
- PostgreSQL database support
- Docker containerization
- Digital Ocean App Platform compatibility